# Render Blueprint — LCB 5K static site
services:
  - type: web
    name: lcb5k-website
    runtime: static
    rootDir: website
    staticPublishPath: .
    buildCommand: echo "Static site — no build required"
